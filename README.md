# Visio landing page

Minimal Next.js + TypeScript site for the Visio installer download.

## Local development

```bash
npm install
npm run dev
```

The installer is served from:

- `public/downloads/visio-setup-1.1.0-x64.exe`

The landing page is implemented in:

- `app/page.tsx`
